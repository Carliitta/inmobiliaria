const initialState={
    inmuebles:[],
}
export function rootReducer(state = initialState, action){
    switch (action.payload) {
   /*      case value:
            
            break;
     */
        default:
            break;
    }
}
